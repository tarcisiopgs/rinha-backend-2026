name: rinha-2026

# Compose oficial de submissão — referencia imagem pré-construída em ghcr.io
# (linux/amd64, RUSTFLAGS=-C target-cpu=haswell -C target-feature=+avx2,+fma,
# dataset embutido em /data/references.bin).
#
# Total: 1 CPU, 350 MB. 2 APIs com 0.40 CPU / 170 MB cada + LB com 0.20 CPU
# / 10 MB. Engine da Rinha bate na porta 9999 (LB). Comunicação api↔lb via
# TCP loopback intra-bridge (UDS sobre tmpfs falha em runners sem io_uring).

services:
  api1:
    image: ghcr.io/tarcisiopgs/rinha-backend-2026:latest
    entrypoint: ["/usr/local/bin/api"]
    environment:
      API_LISTEN: tcp:0.0.0.0:9000
      DATASET_PATH: /data/references.bin
      RUST_LOG: warn
      MONOIO_DRIVER: legacy
    deploy:
      resources:
        limits:
          cpus: "0.40"
          memory: "170MB"
    networks:
      - rinha

  api2:
    image: ghcr.io/tarcisiopgs/rinha-backend-2026:latest
    entrypoint: ["/usr/local/bin/api"]
    environment:
      API_LISTEN: tcp:0.0.0.0:9000
      DATASET_PATH: /data/references.bin
      RUST_LOG: warn
      MONOIO_DRIVER: legacy
    deploy:
      resources:
        limits:
          cpus: "0.40"
          memory: "170MB"
    networks:
      - rinha

  lb:
    image: ghcr.io/tarcisiopgs/rinha-backend-2026:latest
    entrypoint: ["/usr/local/bin/lb"]
    environment:
      LB_LISTEN: 0.0.0.0:9999
      LB_UPSTREAMS: tcp:api1:9000,tcp:api2:9000
      RUST_LOG: warn
      MONOIO_DRIVER: legacy
    ports:
      - "9999:9999"
    depends_on:
      - api1
      - api2
    deploy:
      resources:
        limits:
          cpus: "0.20"
          memory: "10MB"
    networks:
      - rinha

networks:
  rinha:
    driver: bridge
